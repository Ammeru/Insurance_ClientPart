const UserListPage = () => {
    return (
        <main className="min-h-screen flex items-center justify-center bg-gray-50">
            <h1 className="text-3xl font-semibold text-gray-800">Список пользователь(Админ-панель)</h1>
        </main>
    );
};

export default UserListPage;